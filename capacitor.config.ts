import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'tf-java-2026-rcd-fend',
  webDir: 'dist/TF_Java_2026_RCD_Fend/browser',
};

export default config;
